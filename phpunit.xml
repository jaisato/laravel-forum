<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <!-- There is no tests/Unit directory in this project - every test is a
             Pest feature test under tests/Feature. PHPUnit 11 treats a testsuite
             pointing at a missing directory as a fatal configuration error, not
             an empty suite, so declaring it aborted the entire run before a
             single test executed: both ./vendor/bin/phpunit and `php artisan
             test` answered only 'Test directory "tests/Unit" not found'. The
             suite is declared again the day a unit test is actually added. -->
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- Tests run against an in-memory SQLite database so the suite needs
             no database server and leaves nothing behind. Override
             DB_CONNECTION/DB_DATABASE to run them against MySQL. -->
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
